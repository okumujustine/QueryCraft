{
  "name": "electron",
  "version": "1.0.0",
  "description": "",
  "main": "dist/main.js",
  "build:electron": "tsc -p electron/tsconfig.json && electron-builder",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "electron:dev": "wait-on tcp:6295 && electron .",
    "electron:build": "npm run build && electron-builder"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
    "devDependencies": {
    "electron": "^37.3.1",
    "electron-builder": "^25.1.0",
    "typescript": "~5.8.3",
    "wait-on": "^8.0.1"
  }
}
